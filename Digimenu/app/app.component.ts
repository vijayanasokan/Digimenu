import { Component, OnInit } from '@angular/core';
import { NgForm } from '@angular/forms';

@Component({
    // moduleId: module.id,
    selector: "myapp",
    templateUrl: "../app/app.html"
})
export class Appcomponent {

    title = "this is from appcomponent";

    value2 = "sample value34";

   
}